[toolchain]
# Wasmtime 46 requires Rust 1.94. Keep the host and guest toolchains identical.
channel = "1.94.0"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown", "wasm32-wasip2", "aarch64-unknown-linux-gnu"]
