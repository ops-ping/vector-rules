node_modules
dist
# Dev embedding model is provided out-of-band (symlinked for local dev); the
# shipping model is uploaded to the bucket by the Phase 3 deploy tooling.
public/models/*.gguf
# Generated by `npm run seed`; regenerable, uploaded to the bucket by deploy.sh.
public/vrules-rest
